import React from "react";
import Typography from "@/components/shared/typography";

const PartnersPage = () => {
    return (
        <main className="min-h-screen p-6 bg-gray-50 dark:bg-gray-900">
            <Typography variant="heading" className="text-center mb-6">Партньори</Typography>
            <Typography variant="body" className="text-center text-gray-700 dark:text-gray-300">
                Тук ще се показват всички партньори на фирмата. Моделна placeholder страница.
            </Typography>
        </main>
    );
};

export default PartnersPage;
